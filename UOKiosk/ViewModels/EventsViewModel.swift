//
//  EventsViewModel.swift
//  UOKiosk
//
//  Created by Jonathan Paul on 5/26/22.
//

import Foundation

struct EventsViewModel {
    var eventsViewModel: [[EventViewModel]]? // TOOD Have a String on each event that is the formatted date in Tuesday, March 30 formatting as a string
    // Append the event view model to its list while its date is the same as the event before it
}
